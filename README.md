# Kronos Programming Language

A human-readable programming language inspired by Python.

## Features

- Natural language syntax
- Entry points with `__init__`
- Functions with forward references
- Error handling with `try/on error`
- Conditionals with `if/else if/else`
- Loops: `for` and `while`

## Syntax Examples

### Variables
