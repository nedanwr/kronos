# Test: Calling function with too few required arguments should fail
# Expected: Error - Function requires at least X arguments

function greet with name, greeting = "Hello":
    return f"{greeting}, {name}!"

# This should fail - name is required
call greet
